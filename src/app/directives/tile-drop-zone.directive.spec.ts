import { TileDropZoneDirective } from './tile-drop-zone.directive';

describe('TileDropZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new TileDropZoneDirective();
    expect(directive).toBeTruthy();
  });
});
