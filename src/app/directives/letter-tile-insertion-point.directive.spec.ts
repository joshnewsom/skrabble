import { LetterTileInsertionPointDirective } from './letter-tile-insertion-point.directive';

describe('LetterTileInsertionPointDirective', () => {
  it('should create an instance', () => {
    const directive = new LetterTileInsertionPointDirective();
    expect(directive).toBeTruthy();
  });
});
